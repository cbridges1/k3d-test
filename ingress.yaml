apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: placr
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: placr
            port:
              number: 8080